apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-service
  annotations:
    argocd.argoproj.io/sync-wave: "-21"
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - protocol: TCP
      name: {{ .Values.frontend.ports.name }}
      port: {{ .Values.frontend.ports.port }}
      targetPort: {{ .Values.frontend.ports.containerPort }}
  type: ClusterIP
