apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-deployment
  labels:
    app: {{ .Chart.Name }}
  annotations:
    argocd.argoproj.io/sync-wave: "-22" 
spec:
  replicas: {{ .Values.postgresProtocolGateway.replicasCount }}
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.postgresProtocolGateway.image.repository }}:{{ .Values.postgresProtocolGateway.image.tag | default .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.postgresProtocolGateway.image.pullPolicy }}
          ports:
            - name: {{ .Values.postgresProtocolGateway.ports.name }}
              containerPort: {{ .Values.postgresProtocolGateway.ports.containerPort }}
          resources:
            {{- toYaml .Values.postgresProtocolGateway.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          envFrom:
           - secretRef:
               name: tcp-gateway-secret
  